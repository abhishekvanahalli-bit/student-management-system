DB_NAME=student_management_db
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_HOST=localhost
DB_PORT=3306

# Optional: pin the Django secret key so sessions survive server restarts.
# If unset, a random one is generated each time the app starts.
DJANGO_SECRET_KEY=

# Required to run reset_admin.py
ADMIN_USERNAME=Abhishek
ADMIN_EMAIL=abhishek@example.com
ADMIN_RESET_PASSWORD=choose_a_strong_password
